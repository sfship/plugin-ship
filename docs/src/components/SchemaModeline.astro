---
import { Code } from '@astrojs/starlight/components';

interface Props {
  schema: 'ship' | 'flow';
}

const { schema } = Astro.props;
const code = `# yaml-language-server: $schema=https://cdn.jsdelivr.net/npm/@sfship/plugin-ship@beta/lib/schemas/${schema}.schema.json`;
---

<Code lang="yaml" code={code} />
