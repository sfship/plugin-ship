{
  "test": {
    "testsPath": "test/**/*.test.ts"
  },
  "wireit": {
    "build": {
      "dependencies": ["compile", "lint", "copy-assets"]
    },
    "copy-assets": {
      "command": "node -e \"require('fs').cpSync('src/core/flows', 'lib/core/flows', {recursive: true, force: true})\"",
      "dependencies": ["compile"],
      "files": ["src/core/flows/**/*.yml"],
      "output": ["lib/core/flows/**/*.yml"]
    },
    "test": {
      "dependencies": ["test:compile", "test:only", "lint"]
    },
    "test:only": {
      "command": "c8 mocha \"test/**/*.test.ts\"",
      "env": { "FORCE_COLOR": "2" },
      "files": ["test/**/*.ts", "src/**/*.ts", "**/tsconfig.json", ".mocha*", "!*.nut.ts", ".nycrc"],
      "output": []
    }
  }
}
